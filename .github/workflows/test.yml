name: Test

on:
  push:
  pull_request:

jobs:
  powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check PowerShell syntax
        shell: powershell
        run: .\tests\Test-Scripts.ps1 -SyntaxOnly
